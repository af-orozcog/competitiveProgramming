#include<bits/stdc++.h>
#define ll long long
using namespace std;
typedef pair<int,int> pi;
typedef pair<ll,ll> pl;


int main(){
    int n;
    scanf(" %d",&n);
    ll nums[n];
    for(int i = 0; i < n;++i) scanf(" %lld",&nums[i]);
    ll ans = 0;
    return 0;
}
