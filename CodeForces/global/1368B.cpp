#include<bits/stdc++.h>
#define ll long long
using namespace std;

int main(){
    ll k;
    scanf(" %lld",&k);
    if(k < 2000ll){
        --k;
        printf("codeforces");
        while(k--) printf("s");
        puts("");
        return 0;
    }

    return 0;
}
