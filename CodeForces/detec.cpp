#include<iostream>
#include<queue>
using namespace std;
int main(){
	int tama;
	cin >> tama;
	queue<int> dat;
	datos[tama]
	for(int i = 0; i < tama;i++){
		int val;
		cin >> val;
		datos[i] = val;
	}
	int last = 0; 
	int days = 0;
	while(last < tama){
		if(datos[last] -1  != last){
			int upTo = datos[last] - 1;
			for(int i = last; i < upTo; i++){
				
			}
			last = upTo;
		}
		else
			days += 1;
	}
}