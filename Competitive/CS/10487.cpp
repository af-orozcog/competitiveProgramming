#include<vector>
#include<algorithm>
#include<cmath>
#include<iostream>
#define ll long long

using namespace std;

int main(){
	int siz;
	int test = 1;
	while(cin >> siz){
		if(siz == 0)
			break;
		vector<int>nums (siz);
		for(int i = 0; i < siz;++i){
			cin >> nums[i];
		}
		sort(nums.begin(),nums.end());
		int que;
		cin >> que;
		int 
		++test;
	}
}