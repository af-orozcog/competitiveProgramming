#include<bits/stdc++.h>
using namespace std;

int main(){
    int n;
    while(scanf(" %d",&n) == 1 && n){
        unordered_set<string> no;
        for(int i = 0; i < n;++i){
            string temp;cin >> temp;
        }
    }
}
