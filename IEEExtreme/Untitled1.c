#include<bits/stdc++.h>
using namespace std;

int main(){
    int n,k;
    scanf(" %d %d",&n,&k);
    unordered_set<char> seenBefore;
    vector<char> good;
    char cards[n],bet[n];
    for(int i = 0; i < n;++i) scanf(" %c",&cards[i]);
    for(int i = 0; i < n;++i) scanf(" %c",&bet[i]);
    for(int i = 0;i < n;++i){
    }
    sort(good.begin(),good.end());
}
