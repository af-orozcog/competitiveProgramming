#include<bits/stdc++.h>
#define ll long long
using namespace std;
typedef pair<ll,ll> pi;

int main(){
    int n,x,y;
    scanf(" %d %d %d",&n,&x,&y);
    for(int i = 1; i < n;++i){
        ans = 0;
        for(int j = 1; j <= n;++j){
            if(j < x || j > y) if(j + i <= n) ++ans;
            else{
                if(j + i <= n && )
            }
        }
    }
    return 0;
}

